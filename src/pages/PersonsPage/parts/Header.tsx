import { PersonsPageHeader } from "styles/style";

export default function Header() {
    return (
        <header>
            <PersonsPageHeader>Lista użytkowników</PersonsPageHeader>
        </header>
    );
}
